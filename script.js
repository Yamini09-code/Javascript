//js can be included using script tag or poviding link to external file path which has adv that it can cache and gives separation of concerns
//js file gets saved cached by the browser it doesnt need to load it again n again
//two main advantages of putting js in script tag and adding path to external file is -> separation of concerns and caching
//script tag can be used to put js directly or add an external file
//advantages of using separate file using src attribute of script tag -> separation of concerns and caching
//script tag is used to insert js into an html page. can also be used to insert internal or external scripts
console.log("mini") 
console.log(console)